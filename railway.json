{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install --optimize-autoloader && npm ci && npm run build"
    },
    "deploy": {
        "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan view:clear && php artisan storage:link && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100,
        "restartPolicyType": "ON_FAILURE"
    }
}